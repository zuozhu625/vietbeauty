# 越南医疗整形项目 - 前端页面设计开发文档

## 📋 项目概述

越南医疗整形项目是一个基于 Astro + Tailwind CSS 的现代化医疗整形平台，提供医院信息、用户分享、知识问答等服务。项目采用组件化架构，支持响应式设计，为越南用户提供完全本地化的体验。本文档详细记录了前端页面的设计规范和开发实现。

## 🎯 项目信息

- **项目名称**: 越南医疗整形平台
- **官方网站**: https://vietbeauty.top
- **技术栈**: Astro v5.14.1 + Tailwind CSS v3.4.18 + TypeScript
- **主题设计**: Cursor 黑色主题
- **语言支持**: 越南语
- **部署方式**: Astro SSR + Nginx反向代理 + SSL
- **后端API**: http://47.237.79.9:5002

## 🏗️ 技术架构

### 核心技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Astro | v5.14.1 | 静态站点生成框架 |
| Tailwind CSS | v3.4.18 | 原子化CSS框架 |
| TypeScript | - | 类型安全的开发体验 |
| Vite | - | 构建工具 |

### 项目结构

```
/root/越南医疗整形项目/
├── src/                           # 前端源代码
│   ├── components/                # 可复用组件
│   │   └── Navigation.astro      # 导航组件 (6个导航项)
│   ├── layouts/                   # 页面布局
│   │   └── Layout.astro          # 主布局文件
│   ├── pages/                     # 页面文件
│   │   ├── index.astro           # 首页 (Hero + 预览模块)
│   │   ├── sharing.astro         # 用户分享列表 (36/页)
│   │   ├── sharing/[id].astro    # 用户分享详情
│   │   ├── knowledge.astro       # 知识问答列表 (36/页, 6分类)
│   │   ├── knowledge/[id].astro  # 知识问答详情
│   │   ├── reviews.astro         # 医院介绍列表 (36/页, 5城市)
│   │   ├── reviews/[id].astro    # 医院详情页
│   │   ├── services.astro        # 服务内容 (6大服务)
│   │   └── contact.astro         # 联系我们 (2栏布局)
│   ├── styles/                    # 样式文件
│   │   └── global.css            # 全局样式 (Cursor主题)
│   └── utils/                     # 工具函数
│       └── api.js                # 后端API调用封装
├── dist/                          # 构建输出
│   ├── client/                   # 客户端资源
│   └── server/                   # SSR服务
│       └── entry.mjs             # SSR入口 (Node.js)
├── public/                        # 静态资源
├── docs/                          # 项目文档
├── deploy.sh                      # 一键部署脚本
└── package.json                   # 依赖配置
```

## 🎨 设计系统

### 色彩主题 (Cursor 黑色主题)

```css
:root {
  --cursor-bg: #0d1117;           /* 背景色 */
  --cursor-surface: #161b22;      /* 表面色 */
  --cursor-border: #21262d;       /* 边框色 */
  --cursor-text: #f0f6fc;         /* 主文字色 */
  --cursor-text-muted: #8b949e;   /* 次要文字色 */
  --cursor-accent: #58a6ff;       /* 强调色 */
  --cursor-accent-hover: #79c0ff; /* 强调色悬停 */
  --cursor-success: #3fb950;      /* 成功色 */
  --cursor-warning: #d29922;      /* 警告色 */
  --cursor-error: #f85149;        /* 错误色 */
  --cursor-blue: #58a6ff;         /* 蓝色 */
}
```

### 字体系统

- **主字体**: Inter (Google Fonts)
- **备用字体**: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif
- **字体权重**: 300, 400, 500, 600, 700

### 组件设计原则

1. **毛玻璃效果**: 使用 `backdrop-filter: blur(10px)` 创建现代感
2. **渐变文字**: 使用 `bg-clip-text` 创建渐变文字效果
3. **3D卡片**: 使用 `transform` 和 `box-shadow` 创建立体感
4. **响应式设计**: 使用 Tailwind 的响应式类名
5. **动画效果**: 使用 CSS 动画和过渡效果

### 媒体块设计系统 (Media Block)

媒体块是项目中最核心的UI组件，用于展示内容卡片。分为两种类型：

#### 1. 自适应高度媒体块
- **使用场景**: 用户分享页面
- **特点**: 高度根据内容自适应
- **布局**: 网格自动对齐，卡片高度可能不同
- **优势**: 灵活展示不同长度的内容

#### 2. 固定高度媒体块
- **使用场景**: 知识问答页面
- **特点**: 固定高度 340px，确保网格整齐
- **布局**: 使用 Flexbox 控制内部区域
- **优势**: 视觉统一，布局规整

#### 媒体块通用样式
```css
.media-card {
  background: linear-gradient(145deg, rgba(255,255,255,0.1), rgba(255,255,255,0.05));
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 16px;
  padding: 24px;
  transition: all 0.3s ease;
}

.media-card:hover {
  transform: scale(1.05);
  box-shadow: 0 8px 32px rgba(88, 166, 255, 0.3);
}
```

#### 文本截断策略
- **标题**: `line-clamp-2` 显示2行
- **内容**: `line-clamp-3` 或 `line-clamp-4` 显示3-4行
- **单行**: `truncate` 单行截断加省略号

## 📱 页面功能详解

### 页面Hero区域优化 (v1.4.0)

**所有非首页的Hero标题区域已优化**：
- **原高度**: `pt-32 pb-16` (上padding 128px，下padding 64px)
- **新高度**: `pt-20 pb-10` (上padding 80px，下padding 40px)
- **优化幅度**: 缩小约35%，节省88px垂直空间
- **优化效果**: 第一行媒体块可直接展示在首屏，无需滚动

**适用页面**：
- 知识问答 (knowledge.astro)
- 用户分享 (sharing.astro)
- 医院介绍 (reviews.astro)
- 服务内容 (services.astro)
- 联系我们 (contact.astro)

### 1. 首页 (index.astro)

**功能模块**:
- **Hero区域**：主标题、副标题和CTA按钮（首页保持原高度）
  - "Bắt đầu tư vấn" 按钮 → 跳转到 `/contact`
  - "Tìm hiểu thêm" 按钮 → 跳转到 `/reviews`
- **统计数据**：500+医院、10000+成功案例、98%满意度、24/7服务
- **用户分享预览**：最新6条用户分享
- **知识问答预览**：最新6条专业问答
- **医院推荐**：评分最高的6家医院
- **服务内容概览**：6大核心服务简介

**数据获取**:
```javascript
// 用户分享 (最新6条，按创建时间倒序)
const shareResponse = await userShareAPI.getList({ 
  page: 1, limit: 6, status: 'published',
  sort: 'created_at', order: 'DESC'
});

// 知识问答 (最新6条，按创建时间倒序)
const knowledgeResponse = await knowledgeAPI.getList({ 
  page: 1, limit: 6, status: 'published',
  sort: 'created_at', order: 'DESC'
});

// 医院推荐 (评分最高6家，按评分倒序)
const hospitalResponse = await hospitalAPI.getList({ 
  page: 1, limit: 6, status: 'active',
  sort: 'rating', order: 'DESC'
});
```

**技术特点**:
- 响应式网格布局 (1/2/3/4列)
- 动画背景效果 (脉冲动画)
- 交互式卡片设计
- 星级评分显示

### 2. 用户分享 (sharing.astro)

#### 功能概述
用户分享页面展示真实用户的整形经验分享，采用媒体块（Media Block）设计，支持分页浏览。

#### 页面结构
```
┌────────────────────────────────────────┐
│ 标题区域                                │
├────────────────────────────────────────┤
│ 媒体块网格 (3列布局)                   │
│ ┌──────┐ ┌──────┐ ┌──────┐            │
│ │ 块 1 │ │ 块 2 │ │ 块 3 │            │
│ └──────┘ └──────┘ └──────┘            │
│ ┌──────┐ ┌──────┐ ┌──────┐            │
│ │ 块 4 │ │ 块 5 │ │ 块 6 │            │
│ └──────┘ └──────┘ └──────┘            │
├────────────────────────────────────────┤
│ 分页导航                                │
└────────────────────────────────────────┘
```

#### 媒体块设计
**尺寸**: 不固定高度，根据内容自适应

**结构**:
```
┌─────────────────────────────────┐
│ 标题 (line-clamp-2)             │
├─────────────────────────────────┤
│ 内容预览 (line-clamp-3)         │
├─────────────────────────────────┤
│ 评分 ⭐⭐⭐⭐⭐                   │
│ 👍 点赞数 | 👁️ 浏览数 | Chi tiết│
└─────────────────────────────────┘
```

#### 技术实现
- **布局**: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8`
- **媒体块**: `.media-card` 类，玻璃拟态效果
- **响应式**: 移动端1列，平板2列，桌面3列
- **交互**: hover 缩放效果 `hover:scale-105`
- **评分**: SVG 星星图标，支持半星显示
- **分页**: 每页36条数据，支持上一页/下一页导航

#### 数据字段
```typescript
{
  id: number;
  title: string;            // 标题
  content: string;          // 内容
  surgery_type: string;     // 手术类型
  hospital_name: string;    // 医院名称（已移除显示）
  rating: number;           // 评分 (1-5)
  like_count: number;       // 点赞数
  view_count: number;       // 浏览数
  created_at: Date;         // 创建日期（已移除显示）
}
```

#### 样式特点
- 玻璃拟态背景 `backdrop-filter: blur(20px)`
- 渐变边框 `border: 1px solid rgba(255, 255, 255, 0.2)`
- 圆角设计 `border-radius: 16px`
- 过渡动画 `transition: all 0.3s ease`

### 3. 知识问答 (knowledge.astro)

#### 功能概述
知识问答页面展示医疗专家的专业问答内容，采用固定高度的媒体块设计，确保布局整齐统一。

#### 页面结构
```
┌────────────────────────────────────────┐
│ 标题区域                                │
├────────────────────────────────────────┤
│ 分类筛选按钮组                          │
├────────────────────────────────────────┤
│ 问答媒体块网格 (3列布局)               │
│ ┌──────┐ ┌──────┐ ┌──────┐            │
│ │ 块 1 │ │ 块 2 │ │ 块 3 │  固定340px │
│ └──────┘ └──────┘ └──────┘            │
│ ┌──────┐ ┌──────┐ ┌──────┐            │
│ │ 块 4 │ │ 块 5 │ │ 块 6 │  固定340px │
│ └──────┘ └──────┘ └──────┘            │
├────────────────────────────────────────┤
│ 分页导航 + 浮动提问按钮                │
└────────────────────────────────────────┘
```

#### 媒体块设计（固定高度版本）
**尺寸**: 固定高度 340px

**结构**:
```
┌─────────────────────────────────┐
│ 问题标题 (2行, 48px)            │ 固定
│ + 分类标签                      │
├─────────────────────────────────┤
│ 回答内容 (3行, 64px)            │ 固定
├─────────────────────────────────┤
│ 👍👁️ | BS 医生 | Chi tiết →    │ 固定单行
└─────────────────────────────────┘
```

#### 底部区域布局（三栏设计）
```
┌─────────────────────────────────────────┐
│ 👍 128  👁️ 256 │ BS 医生名 │ Chi tiết → │
└─────────────────────────────────────────┘
   左：统计数据    中：医生信息   右：链接
```

#### 技术实现
- **固定高度**: `h-[340px]` 确保所有卡片高度一致
- **Flexbox布局**: `flex flex-col` 垂直排列
- **区域控制**:
  - 标题区 `flex-shrink-0` - 不压缩
  - 内容区 `flex-grow` - 可伸缩
  - 底部区 `flex-shrink-0` - 不压缩
- **文本截断**:
  - 问题标题: `line-clamp-2 h-12` (2行)
  - 回答内容: `line-clamp-3 h-16` (3行)
  - 医生名称: `truncate max-w-[100px]`

#### 数据字段
```typescript
{
  id: number;
  question: string;         // 问题
  answer: string;           // 回答
  category: string;         // 分类
  doctor_name: string;      // 医生名称
  like_count: number;       // 点赞数
  view_count: number;       // 浏览数
  created_at: Date;         // 创建日期
}
```

#### 分类筛选 (v1.4.0 已优化)
**布局优化**：
- **原布局**: `flex-wrap` 多行折叠
- **新布局**: `overflow-x-auto` 横向滚动
- **优化效果**: 所有标签在一行显示，支持左右滑动
- **标签样式**: 所有标签添加 `whitespace-nowrap` 防止换行

支持的分类（7个）：
- Tất cả câu hỏi (所有问题)
- Thẩm mỹ khuôn mặt (面部美容)
- Thẩm mỹ ngực (胸部美容)
- Hút mỡ tạo dáng (吸脂塑形)
- Phục hồi sau sinh (产后修复)
- Niềng răng (牙齿矫正)
- **Tư vấn bệnh viện** (医院问答) - 🆕 DQA自动生成内容，绿色主题

#### 特殊功能
- **分页**: 每页36条数据
- **医生头像**: 渐变圆形头像，显示 "BS" 标识
- **统计信息**: 点赞数和浏览数实时显示
- **分类筛选**: 动态URL参数 `/knowledge?category=xxx`
- **DQA集成**: 医院问答自动生成，每15分钟新增1条（v1.4.0新增）

**DQA医院问答功能 (v1.4.0)**:
- **分类标签**: "Tư vấn bệnh viện"（绿色主题，区别于其他蓝色标签）
- **访问地址**: `/knowledge?category=Tư%20vấn%20bệnh%20viện`
- **内容来源**: DQA系统自动生成，基于122家医院真实数据
- **问答类型**: 6种（资质、等级、服务、地址、联系、医生）
- **更新频率**: 每15分钟自动生成1条新问答
- **数据标识**: `source: 'api'`, `category: 'Tư vấn bệnh viện'`

**已移除功能**:
- ❌ 右下角浮动提问按钮

#### 样式特点
- 固定高度确保网格对齐
- 单行底部布局，空间利用更高效
- 分类标签使用蓝色主题 `bg-blue-500/20`
- 医生头像使用渐变 `from-blue-400 to-purple-400`

### 4. 医院介绍 (reviews.astro)

#### 功能概述
医院介绍页面展示122家合作医院信息，支持城市筛选和分页浏览。

#### 页面结构
```
┌────────────────────────────────────────┐
│ 标题区域                                │
├────────────────────────────────────────┤
│ 城市筛选按钮组 (5个城市)               │
├────────────────────────────────────────┤
│ 医院媒体块网格 (3列布局)               │
│ ┌──────┐ ┌──────┐ ┌──────┐            │
│ │医院1 │ │医院2 │ │医院3 │            │
│ └──────┘ └──────┘ └──────┘            │
├────────────────────────────────────────┤
│ 分页导航 (36家/页)                     │
└────────────────────────────────────────┘
```

**城市筛选**:
- Tất cả (全部)
- TP.HCM (胡志明市) - 最多
- Hà Nội (河内)
- Đà Nẵng (岘港)
- Hải Phòng (海防)
- Cần Thơ (芹苴)

**技术实现**:
- 响应式3列网格布局
- 星级评分显示 (4.0-4.8分)
- URL参数筛选: `/reviews?city=TP.HCM`
- 分页导航: `/reviews?page=2`
- 浏览数实时显示

**详情页 (reviews/[id].astro)**:
- 医院基本信息展示
- 联系方式 (电话、邮箱、网站)
- 4个媒体块:
  - Dịch vụ cung cấp (提供服务)
  - Trang thiết bị (设施设备)
  - Chứng nhận chất lượng (资质认证)
  - Đánh giá nổi bật (用户评价)
- 相关医院推荐 (同城市)
- 浏览数自动+1

**数据字段**:
```typescript
{
  id: number;
  name: string;              // 医院名称
  description: string;       // 医院描述
  address: string;           // 地址
  city: string;              // 城市
  phone: string;             // 电话
  email: string;             // 邮箱
  website: string;           // 网站
  rating: number;            // 评分 (4.0-4.8)
  review_count: number;      // 评价数
  specialties: string[];     // 专科 (JSON)
  services: string[];        // 服务 (JSON)
  facilities: string[];      // 设施 (JSON)
  certifications: string[];  // 认证 (JSON)
  level: string;             // 等级 (A+/A/B)
  view_count: number;        // 浏览数
}
```

### 5. 服务内容 (services.astro)

#### 功能概述
服务内容页面展示6大核心服务，每个服务包含详细功能说明和数据高亮。

#### 页面结构
```
┌────────────────────────────────────────┐
│ 标题区域 + 统计数据                     │
├────────────────────────────────────────┤
│ 6个服务卡片 (3列网格, 固定高度)        │
│ ┌──────┐ ┌──────┐ ┌──────┐            │
│ │服务1 │ │服务2 │ │服务3 │            │
│ └──────┘ └──────┘ └──────┘            │
│ ┌──────┐ ┌──────┐ ┌──────┐            │
│ │服务4 │ │服务5 │ │服务6 │            │
│ └──────┘ └──────┘ └──────┘            │
├────────────────────────────────────────┤
│ 工作流程 (4步骤)                        │
├────────────────────────────────────────┤
│ 优势展示 (4个优势)                      │
├────────────────────────────────────────┤
│ 客户评价 (3个评价)                      │
├────────────────────────────────────────┤
│ CTA行动号召区                           │
└────────────────────────────────────────┘
```

**6大核心服务**:
1. ✅ **Đánh giá rủi ro & tư vấn gói làm đẹp** (风险评估与美容套餐咨询)
   - 6个功能点：评估健康、分析风险、方法对比、费用估算、恢复时间、套餐推荐
   - 数据高亮：95%准确率、30-45分钟、免费咨询

2. 💬 **Tư vấn & giải đáp thắc mắc thẩm mỹ** (美容咨询与疑问解答)
   - 6个功能点：术前准备、流程解答、术后护理、价格对比、经验分享、24/7支持
   - 数据高亮：<2小时响应、50+专家、24/7在线

3. 📅 **Hỗ trợ đặt lịch bệnh viện** (医院预约支持)
   - 6个功能点：按城市找医院、预算筛选、医院对比、快速预约、准备指导、自动提醒
   - 数据高亮：120+医院、免费预约、即时确认

4. 🔍 **Kiểm tra & xác minh thông tin bệnh viện** (医院信息验证)
   - 6个功能点：验证执照、医生认证、设备评估、卫生检查、真实评价、问题预警
   - 数据高亮：5000+档案、每周更新、98%可信度

5. 💳 **Tư vấn trả góp chi phí y tế** (医疗费用分期咨询)
   - 6个功能点：银行对比、利率计算、付款计划、贷款指导、贷款条件、财务协助
   - 数据高亮：15+银行、0%起息、24小时审批

6. ⚖️ **Hỗ trợ khiếu nại & bảo vệ quyền lợi** (投诉支持与权益保护)
   - 6个功能点：权益咨询、准备文件、收集证据、提交投诉、律师对接、跟踪进度
   - 数据高亮：87%成功率、20+律师、免费法律咨询

**卡片设计特点**:
- ✅ 固定高度一致 (使用 `flex flex-col h-full`)
- ✅ 渐变色彩头部 (每个服务独特配色)
- ✅ 功能列表 (6个功能点 + 绿色勾选图标)
- ✅ 底部对齐 (使用 `mt-auto` 自动推到底部)
- ✅ 3个关键数据高亮
- ✅ 渐变按钮 "Tìm hiểu thêm →"

**布局技术**:
```css
父容器: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3

卡片结构:
.media-card.flex.flex-col.h-full
  ├─ Header (flex-shrink-0) - 图标渐变区
  └─ Content (flex-grow)
      ├─ 标题描述 (flex-shrink-0, h-14 + h-20)
      ├─ 功能列表 (flex-shrink-0)
      └─ 底部区 (mt-auto flex-shrink-0)
          ├─ 数据高亮 (grid-cols-3)
          └─ 操作按钮
```

### 6. 联系我们 (contact.astro)

#### 功能概述
联系我们页面采用2栏横向布局，展示联系方式和办公地址，简洁明了。

#### 页面结构
```
┌──────────────────────────────────────────────┐
│ 标题区域                                      │
├──────────────────────────────────────────────┤
│ ┌──────────────────┐  ┌──────────────────┐  │
│ │  联系信息板块    │  │  办公地址板块    │  │
│ │  ┌────────────┐  │  │  ┌────────────┐  │  │
│ │  │📞 Hotline  │  │  │  │🏢 TP.HCM   │  │  │
│ │  │📧 Email    │  │  │  │🏢 Hà Nội   │  │  │
│ │  │💬 WeChat   │  │  │  │🏢 Đà Nẵng  │  │  │
│ │  │⏰ 工作时间  │  │  │              │  │  │
│ │  └────────────┘  │  │  └────────────┘  │  │
│ └──────────────────┘  └──────────────────┘  │
└──────────────────────────────────────────────┘
```

**联系方式板块**:
- 📞 **Hotline 24/7**: 两个咨询电话
- 📧 **Email**: 两个邮箱地址
- 💬 **WeChat咨询**: WeChat ID和说明
- ⏰ **工作时间**: 周一到周日 8:00-20:00

**办公地址板块**:
- 🏢 **Văn phòng TP.HCM** (胡志明市办公室)
  - 完整地址
  - 地铁交通指引
  - 公交线路
  
- 🏢 **Văn phòng Hà Nội** (河内办公室)
  - 完整地址
  - 地铁交通指引
  - 公交线路
  
- 🏢 **Văn phòng Đà Nẵng** (岘港办公室)
  - 完整地址
  - 机场交通信息
  - 公交线路

**布局技术**:
```html
<!-- 2列响应式布局 -->
<div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
  <div class="glass">联系信息</div>
  <div class="glass">办公地址</div>
</div>

响应式:
- 移动端/平板: 竖向单列
- 桌面端(≥1024px): 横向双列
```

**已移除的功能**:
- ❌ 在线咨询表单
- ❌ Google地图集成
- ❌ 社交媒体链接
- ❌ 右下角悬浮按钮

## 🧩 组件系统

### Navigation.astro (导航组件)

**功能**:
- 响应式导航栏
- Logo展示
- 动态高亮当前页面
- 移动端菜单

**Props**:
- 无 (使用 Astro.url.pathname 获取当前路径)

**动态高亮逻辑**:
```typescript
const currentPath = Astro.url.pathname;

const isActive = (path: string) => {
  if (path === '/') {
    return currentPath === '/';  // 首页精确匹配
  }
  return currentPath.startsWith(path);  // 其他页面前缀匹配
};

const getNavClass = (path: string) => {
  const baseClass = "hover:text-cursor-accent transition-colors duration-300 px-3 py-2 rounded-md text-sm font-medium";
  const activeClass = "text-cursor-accent";  // 高亮色
  const inactiveClass = "text-cursor-text-muted";  // 暗淡色
  
  return `${baseClass} ${isActive(path) ? activeClass : inactiveClass}`;
};
```

**导航项**:
- Trang chủ (首页) - `/`
- Chia sẻ người dùng (用户分享) - `/sharing`
- Hỏi đáp (知识问答) - `/knowledge`
- Giới thiệu bệnh viện (医院介绍) - `/reviews`
- Nội dung dịch vụ (服务内容) - `/services`
- Liên hệ (联系我们) - `/contact`

**样式类**:
- `fixed top-0 w-full z-50` - 固定定位
- `backdrop-blur-md bg-black/20` - 毛玻璃效果
- `border-b border-white/10` - 底部边框

**高亮效果**:
- 当前页面的导航项显示蓝色高亮 `text-cursor-accent`
- 其他页面显示灰色 `text-cursor-text-muted`
- 包括详情页在内的子页面也会高亮对应的列表页导航

### Layout.astro (主布局)

**功能**:
- 页面结构定义
- 导航组件集成
- 页脚信息
- 全局样式引入

**Props**:
- `title: string` - 页面标题
- `description?: string` - 页面描述

## 🎨 样式系统

### 全局样式 (global.css)

**核心样式类**:

1. **毛玻璃效果**:
```css
.glass {
  background: var(--cursor-surface);
  backdrop-filter: blur(10px);
  border: 1px solid var(--cursor-border);
  border-radius: 12px;
}
```

2. **渐变文字**:
```css
.gradient-text-blue {
  background: linear-gradient(135deg, var(--cursor-blue) 0%, var(--cursor-accent) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
```

3. **按钮样式**:
```css
.btn-primary {
  background: var(--cursor-accent);
  color: var(--cursor-bg);
  border: none;
  padding: 12px 24px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(88, 166, 255, 0.3);
}
```

4. **媒体卡片**:
```css
.media-card {
  background: linear-gradient(145deg, rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.05));
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 16px;
  padding: 24px;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}
```

### Tailwind 配置 (tailwind.config.mjs)

**自定义颜色**:
```javascript
colors: {
  cursor: {
    bg: '#0d1117',
    surface: '#161b22',
    border: '#21262d',
    text: '#f0f6fc',
    'text-muted': '#8b949e',
    accent: '#58a6ff',
    'accent-hover': '#79c0ff',
    success: '#3fb950',
    warning: '#d29922',
    error: '#f85149',
    blue: '#58a6ff',
  },
}
```

## 📱 响应式设计

### 断点系统

| 断点 | 屏幕尺寸 | 用途 |
|------|----------|------|
| sm | ≥640px | 小屏幕 |
| md | ≥768px | 中等屏幕 |
| lg | ≥1024px | 大屏幕 |
| xl | ≥1280px | 超大屏幕 |

### 响应式布局示例

```html
<!-- 网格布局 -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
  <!-- 卡片内容 -->
</div>

<!-- 弹性布局 -->
<div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
  <!-- 内容 -->
</div>
```

## 🚀 开发工作流

### 本地开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 访问地址
http://localhost:4321
```

### 构建部署

```bash
# 构建生产版本
npm run build

# 预览构建结果
npm run preview

# 一键部署
./deploy.sh
```

### 代码规范

1. **文件命名**: 使用 kebab-case (如: `user-profile.astro`)
2. **组件命名**: 使用 PascalCase (如: `Navigation.astro`)
3. **CSS类名**: 优先使用 Tailwind 类名
4. **注释**: 使用越南语注释
5. **缩进**: 使用 2 个空格

## 🔧 开发工具

### 推荐 VS Code 扩展

- Astro
- Tailwind CSS IntelliSense
- TypeScript Importer
- Auto Rename Tag
- Bracket Pair Colorizer

### 调试技巧

1. **浏览器开发者工具**: 检查响应式布局
2. **Astro Dev Tools**: 组件调试
3. **Tailwind CSS IntelliSense**: 类名提示
4. **控制台日志**: 调试 JavaScript

## 📊 性能优化

### 构建优化

1. **静态生成**: 使用 Astro 的静态生成功能
2. **代码分割**: 自动代码分割
3. **图片优化**: 使用 WebP 格式
4. **CSS 优化**: 自动 CSS 压缩

### 运行时优化

1. **懒加载**: 图片和组件懒加载
2. **缓存策略**: 静态资源缓存
3. **CDN**: 使用 CDN 加速
4. **压缩**: Gzip 压缩

## 🐛 常见问题

### 1. 样式不生效

**原因**: Tailwind 类名未正确配置
**解决**: 检查 `tailwind.config.mjs` 配置

### 2. 组件不显示

**原因**: 导入路径错误
**解决**: 检查相对路径是否正确

### 3. 响应式布局问题

**原因**: 断点类名使用错误
**解决**: 检查 Tailwind 响应式类名

### 4. 构建失败

**原因**: 依赖版本冲突
**解决**: 删除 `node_modules` 重新安装

## 📈 未来规划

### 短期目标

1. 添加更多动画效果
2. 优化移动端体验
3. 增加搜索功能
4. 添加用户认证

### 长期目标

1. 多语言支持
2. 内容管理系统
3. 用户评论系统
4. 在线预约功能

## 🌐 部署和访问

### 生产环境

**访问地址**:
- 官方域名: https://vietbeauty.top (推荐)
- 备用域名: https://www.vietbeauty.top
- IP地址: http://47.237.79.9:5001

**部署方式**:
- Astro SSR模式 (服务器端渲染)
- Nginx反向代理
- SSL证书 (Let's Encrypt)
- systemd进程管理

**部署命令**:
```bash
# 一键部署
cd /root/越南医疗整形项目
./deploy.sh

# 仅更新前端
systemctl stop vietnam-medical.service
npm run build
systemctl start vietnam-medical.service
```

### 开发环境

**本地开发**:
```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 访问地址
http://localhost:4321
```

**环境配置**:
- 开发环境: `npm run dev` (热重载)
- 预览构建: `npm run preview`
- 生产构建: `npm run build`

## 🔗 API集成

### 后端API调用

**API基础URL**: `http://127.0.0.1:5002/api`

**封装工具** (`src/utils/api.js`):
```javascript
// 用户分享API
export const userShareAPI = {
  getList(params),    // 获取列表
  getById(id),        // 获取详情
  like(id)            // 点赞
};

// 知识问答API
export const knowledgeAPI = {
  getList(params),    // 获取列表
  getById(id),        // 获取详情
  like(id)            // 点赞
};

// 医院API
export const hospitalAPI = {
  getList(params),    // 获取列表
  getById(id)         // 获取详情
};
```

**请求示例**:
```javascript
// 在页面顶层代码中调用
const response = await userShareAPI.getList({
  page: 1,
  limit: 36,
  status: 'published',
  sort: 'created_at',
  order: 'DESC'
});

const items = response.data || [];
const pagination = response.pagination || {};
```

**错误处理**:
```javascript
try {
  const response = await hospitalAPI.getList(params);
  hospitals = response.data || [];
} catch (error) {
  console.error('获取数据失败:', error);
  // 使用默认数据或显示错误提示
}
```

## 📊 SEO优化

### Meta标签配置

**Layout.astro**:
```astro
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width" />
<meta name="description" content={description} />
<title>{title}</title>

<!-- Open Graph -->
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:type" content="website" />
<meta property="og:url" content="https://vietbeauty.top" />
```

**页面标题**:
- 首页: "Phẫu thuật Thẩm mỹ Việt Nam - Nền tảng..."
- 用户分享: "Chia sẻ người dùng - Phẫu thuật..."
- 知识问答: "Hỏi đáp - Phẫu thuật..."
- 医院介绍: "Giới thiệu bệnh viện - Phẫu thuật..."
- 服务内容: "Nội dung dịch vụ - Phẫu thuật..."
- 联系我们: "Liên hệ - Phẫu thuật..."

## 📞 技术支持

- **项目维护**: 开发团队
- **官方网站**: https://vietbeauty.top
- **文档更新**: 2025-10-11
- **版本**: v1.4.0

## 📝 更新记录

### v1.4.0 (2025-10-11)
- ✅ 新增DQA医院自动问答系统集成
- ✅ 知识问答页面添加"Tư vấn bệnh viện"分类标签
- ✅ 优化所有非首页Hero区域高度（缩小35%）
- ✅ 优化标签布局为横向滚动（确保一行显示）
- ✅ 移除医院问答标签icon
- ✅ 所有标签添加whitespace-nowrap防止换行
- ✅ 优化筛选区域间距（pb-16 → pb-12）

### v1.3.0 (2025-10-10)
- ✅ 重新设计服务页面 (6大服务)
- ✅ 简化联系我们页面 (2栏布局)
- ✅ 移除悬浮按钮
- ✅ 首页按钮添加跳转
- ✅ 优化媒体块布局一致性
- ✅ 配置域名和SSL

### v1.2.0 (2024-10-08)
- ✅ SSR模式部署
- ✅ 后端API集成
- ✅ 动态数据加载

### v1.1.0 (2024-10-07)
- ✅ 组件系统优化
- ✅ 导航组件重构
- ✅ 响应式设计改进

### v1.0.0 (2024-10-06)
- ✅ 初始版本发布
- ✅ Cursor主题应用
- ✅ 基础页面开发

---

**注意**: 本文档与生产环境代码保持同步，定期更新。如有疑问请参考 [项目架构开发文档](./项目架构开发文档.md)。
